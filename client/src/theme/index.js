import { purple } from '@material-ui/core/colors';

// placeholder for MUI theme overrides
const theme = {
    palette: {
        primary: purple,
        secondary: purple,
        accent: purple,
        type: 'light',
    },
  };
  
  export default theme;